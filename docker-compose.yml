services:
  postgres:
    build: ./docker/postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U matchbox -d matchbox_test"]
      interval: 5s
      timeout: 5s
      retries: 10
